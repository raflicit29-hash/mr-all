MR ALL V7 SIDEBAR UPDATE

Basis:
- Latest available MR ALL V5 cash-fix HTML in the working files.
- New supplied MR ALL IN logo.

Changes:
- Added collapsible left sidebar.
- Sidebar buttons map to the existing pages/features in the HTML.
- Sidebar can be hidden and reopened with the arrow/menu button.
- Main content shifts instead of being covered by the sidebar.
- Replaced the header logo with the supplied MR ALL IN logo.
- Existing localStorage/JSON data logic in index.html is preserved; this UI update does not intentionally reset application data.

Important:
- This is the updated web/PWA package, not a rebuilt APK binary.
- To update an APK, the HTML/assets must be placed into the Android project's assets and the APK rebuilt/signing performed.
